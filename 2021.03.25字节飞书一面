> 2021/03/25
>
> 一面
>
> 网络：
>
> tcp和udp的区别；三次握手、四次挥手；
>
> HTTP请求报文、请求方法
>
> 输入url发生了什么
>
> 数据库：
>
> 索引的本质、优点、底层结构、为什么要用索引
>
> 引擎（Innodb和MyisAM的区别）
>
> char和varchar；
>
> 操作系统：
>
> 进程和线程的区别，线程间通信方式？
>
> 死锁是什么举例具体说说，怎么解决？
>
> 语言：
>
> c++多态、继承（自己的理解）、虚函数
>
> python:切片是什么，怎么取最后一个值，拉姆达表达式
>
> 算法题：
>
> rand7()为生成均匀的1，7设计rand10()，生成均匀的1，10
>
> ```
> int rand10(){
> 	int i=rand7()-1;
> 	int j=rand7()-1;
> 	int num=i*7+j;
> 	if(num>=40){
> 		return rand10();
> 	}else{
> 		return num%10+1;
> 	}
> }
> ```
>
> 

